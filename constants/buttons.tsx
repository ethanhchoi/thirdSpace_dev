import { StyleSheet, Text, View, Button} from 'react-native'
import React from 'react'

//Params consist of (colorStyle of Page, direct next to where)
export const Tutorial_Next_Button = ({setColor,page}:{setColor:string,page:string}) => {
  return (
    <Button title = "Next Button"/>
  )
}



const styles = StyleSheet.create(
  {}
)